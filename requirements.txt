# Core data stack (Days 1 to 3)
numpy==2.1.3
scipy==1.15.3
# ^ scipy is a scikit-learn dependency. It is pinned explicitly because leaving it
#   unpinned lets a later install drift it to a build that expects a newer numpy,
#   which surfaces as "cannot import name 'matvec' from 'numpy._core'".
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.5.2
jupyter==1.1.1
ipykernel==6.29.5

# Machine learning models (Day 3)
# Pinned to versions verified against numpy 2.1.3 / scikit-learn 1.5.2 above.
xgboost==3.3.0
lightgbm==4.6.0
mlxtend==0.23.4

# Database connectivity (Postgres is our single source of truth)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Analytics engineering (Days 4 to 5)
# NOTE: dbt-core and dbt-postgres are versioned INDEPENDENTLY (there is no dbt-postgres 1.8.9).
# 1.10.x is the first line that installs cleanly on Python 3.11, 3.12 and 3.13.
dbt-core==1.10.22
dbt-postgres==1.10.2

# Orchestration (Day 5: Airflow primary, Dagster comparative)
# Installed inside their own containers via compose, not here, to keep this image light.
