/*
    LAB 2, STEP 5.  The Day 3 modelling table, rebuilt as a governed model.

    Same eleven columns as data/processed/service_requests_features.csv.
    Expect 12,000 rows against the CSV's 12,027. That difference is the point.
*/

-- TODO: select sla_met, resolution_hours, submitted_hour, submitted_dow,
--       submitted_month, is_weekend, is_digital, population, priority_rank,
--       target_resolution_hours and satisfaction_score from int_requests_enriched.

select 1 as todo
