/*
    LAB 2, STEP 3.  Business question 1: are we meeting our SLAs?
    Grain: one row per service per month.  Target: exactly 140 rows.
*/

-- TODO: aggregate int_requests_enriched by month and service.
--       Report the request count, how many were judged, the SLA rate, the mean
--       and median resolution time, mean satisfaction, and how many are still open.

select 1 as todo
