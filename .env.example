# Copy to .env (PowerShell: Copy-Item .env.example .env) and adjust if needed.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=ads_dbt
POSTGRES_PASSWORD=ads_dbt_password
POSTGRES_DB=ads_dbt_analytics
